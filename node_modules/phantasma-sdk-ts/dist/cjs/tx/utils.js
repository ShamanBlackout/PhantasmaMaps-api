"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPrivateKeyFromWif = getPrivateKeyFromWif;
exports.getAddressFromWif = getAddressFromWif;
exports.getPublicKeyFromPrivateKey = getPublicKeyFromPrivateKey;
exports.generateNewSeed = generateNewSeed;
exports.generateNewSeedWords = generateNewSeedWords;
exports.generateNewWif = generateNewWif;
exports.getWifFromPrivateKey = getWifFromPrivateKey;
exports.signData = signData;
exports.verifyData = verifyData;
const wif_1 = require("wif");
const bs58_1 = __importDefault(require("bs58"));
const bip39 = __importStar(require("bip39"));
const crypto_1 = __importDefault(require("crypto"));
const ed25519_js_1 = require("../types/ed25519.js");
const index_js_1 = require("../utils/index.js");
function ab2hexstring(arr) {
    if (typeof arr !== 'object') {
        throw new Error(`ab2hexstring expects an array.Input was ${arr}`);
    }
    let result = '';
    const intArray = new Uint8Array(arr);
    for (const i of intArray) {
        let str = i.toString(16);
        str = str.length === 0 ? '00' : str.length === 1 ? '0' + str : str;
        result += str;
    }
    return result;
}
function getPrivateKeyFromWif(wif) {
    return ab2hexstring((0, wif_1.decode)(wif, 128).privateKey);
}
function getAddressFromWif(wif) {
    const privateKey = getPrivateKeyFromWif(wif);
    const publicKey = getPublicKeyFromPrivateKey(privateKey);
    const addressHex = Buffer.from('0100' + publicKey, 'hex');
    return 'P' + bs58_1.default.encode(Uint8Array.from(addressHex));
}
function getPublicKeyFromPrivateKey(privateKey) {
    return (0, ed25519_js_1.getEd25519PublicKeyHex)(privateKey);
}
function generateNewSeed() {
    const buffer = new Uint8Array(32);
    const privateKey = Buffer.alloc(32);
    crypto_1.default.getRandomValues(buffer);
    for (let i = 0; i < 32; ++i) {
        privateKey.writeUInt8(buffer[i], i);
    }
    const mnemonic = bip39.generateMnemonic();
    return mnemonic;
}
function generateNewSeedWords() {
    const buffer = new Uint8Array(32);
    const privateKey = Buffer.alloc(32);
    crypto_1.default.getRandomValues(buffer);
    for (let i = 0; i < 32; ++i) {
        privateKey.writeUInt8(buffer[i], i);
    }
    const mnemonic = bip39.generateMnemonic();
    const seedWords = mnemonic.split(' ');
    return seedWords;
}
function generateNewWif() {
    const buffer = new Uint8Array(32);
    const privateKey = Buffer.alloc(32);
    crypto_1.default.getRandomValues(buffer);
    for (let i = 0; i < 32; ++i) {
        privateKey.writeUInt8(buffer[i], i);
    }
    const wif = (0, wif_1.encode)({
        version: 128,
        privateKey: Uint8Array.from(privateKey),
        compressed: true,
    });
    return wif;
}
function getWifFromPrivateKey(privateKey) {
    const privateKeyBuffer = Buffer.from(privateKey, 'hex');
    const wif = (0, wif_1.encode)({
        version: 128,
        privateKey: Uint8Array.from(privateKeyBuffer),
        compressed: true,
    });
    return wif;
}
function signData(msgHex, privateKey) {
    const sig = (0, ed25519_js_1.signEd25519)((0, index_js_1.hexToBytes)(msgHex), (0, index_js_1.hexToBytes)(privateKey));
    const numBytes = sig.length;
    return '01' + (numBytes < 16 ? '0' : '') + numBytes.toString(16) + (0, index_js_1.bytesToHex)(sig).toUpperCase();
}
function verifyData(msgHex, phaSig, address) {
    const msgBytes = Buffer.from(msgHex, 'hex');
    const realSig = phaSig.substring(4);
    const pubKey = bs58_1.default.decode(address.substring(1)).slice(2);
    return (0, ed25519_js_1.verifyEd25519)(Uint8Array.from(msgBytes), (0, index_js_1.hexToBytes)(realSig), Uint8Array.from(pubKey));
}
