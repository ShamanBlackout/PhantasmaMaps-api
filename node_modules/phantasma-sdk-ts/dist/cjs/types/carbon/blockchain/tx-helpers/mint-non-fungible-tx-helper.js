"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MintNonFungibleTxHelper = void 0;
const index_js_1 = require("../../../../utils/index.js");
const carbon_serialization_js_1 = require("../../../carbon-serialization.js");
const bytes32_js_1 = require("../../bytes32.js");
const small_string_js_1 = require("../../small-string.js");
const tx_types_js_1 = require("../../tx-types.js");
const tx_msg_signer_js_1 = require("../extensions/tx-msg-signer.js");
const token_helper_js_1 = require("../modules/token-helper.js");
const tx_msg_js_1 = require("../tx-msg.js");
const tx_msg_mint_non_fungible_js_1 = require("../tx-msg-mint-non-fungible.js");
const fee_options_js_1 = require("./fee-options.js");
class MintNonFungibleTxHelper {
    // Build a Tx without signing
    static buildTx(carbonTokenId, carbonSeriesId, senderPublicKey, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const fees = feeOptions ?? new fee_options_js_1.MintNftFeeOptions();
        const maxGas = fees.calculateMaxGas(1);
        const msg = new tx_msg_js_1.TxMsg();
        msg.type = tx_types_js_1.TxTypes.MintNonFungible;
        msg.expiry = expiry ?? BigInt(Date.now() + 60000);
        msg.maxGas = maxGas;
        msg.maxData = maxData ?? 0n;
        msg.gasFrom = senderPublicKey;
        msg.payload = small_string_js_1.SmallString.empty;
        const mint = new tx_msg_mint_non_fungible_js_1.TxMsgMintNonFungible();
        mint.tokenId = carbonTokenId;
        mint.seriesId = carbonSeriesId;
        mint.to = receiverPublicKey;
        mint.rom = rom;
        mint.ram = ram;
        msg.msg = mint;
        return msg;
    }
    // Build and sign, returning raw bytes
    static buildTxAndSign(tokenId, seriesId, signer, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const senderPub = new bytes32_js_1.Bytes32(signer.publicKey);
        const tx = this.buildTx(tokenId, seriesId, senderPub, receiverPublicKey, rom, ram, feeOptions, maxData, expiry);
        return tx_msg_signer_js_1.TxMsgSigner.signAndSerialize(tx, signer);
    }
    // Build, sign and return hex string
    static buildTxAndSignHex(tokenId, seriesId, signer, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const bytes = this.buildTxAndSign(tokenId, seriesId, signer, receiverPublicKey, rom, ram ?? new Uint8Array(), feeOptions, maxData, expiry);
        return (0, index_js_1.bytesToHex)(bytes);
    }
    static parseResult(carbonTokenId, resultHex) {
        const result = [];
        const r = new carbon_serialization_js_1.CarbonBinaryReader((0, index_js_1.hexToBytes)(resultHex));
        const count = r.read4u();
        for (let i = 0; i < count; i++) {
            const instanceId = r.read8u();
            const carbonNftId = token_helper_js_1.TokenHelper.getNftAddress(carbonTokenId, instanceId);
            result.push(carbonNftId);
        }
        return result;
    }
}
exports.MintNonFungibleTxHelper = MintNonFungibleTxHelper;
