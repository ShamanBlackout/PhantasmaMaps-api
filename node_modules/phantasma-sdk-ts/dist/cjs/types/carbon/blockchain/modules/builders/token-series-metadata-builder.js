"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TokenSeriesMetadataBuilder = void 0;
const carbon_serialization_js_1 = require("../../../../carbon-serialization.js");
const index_js_1 = require("../../vm/index.js");
const standard_meta_js_1 = require("../standard-meta.js");
const metadata_helper_js_1 = require("./metadata-helper.js");
class TokenSeriesMetadataBuilder {
    static buildAndSerialize(seriesMetadataSchema, newPhantasmaSeriesId, metadata) {
        const wMetadata = new carbon_serialization_js_1.CarbonBinaryWriter();
        const sharedRomField = (0, metadata_helper_js_1.findMetadataField)(metadata, 'rom');
        let rom = new Uint8Array();
        if (sharedRomField) {
            if (!(sharedRomField.value instanceof Uint8Array || typeof sharedRomField.value === 'string')) {
                throw Error("'rom' must be a byte array or hex string");
            }
            rom = sharedRomField.value;
        }
        const seriesMetadata = new index_js_1.VmDynamicStruct();
        seriesMetadata.fields = [
            index_js_1.VmNamedDynamicVariable.from(standard_meta_js_1.StandardMeta.id, index_js_1.VmType.Int256, newPhantasmaSeriesId),
            index_js_1.VmNamedDynamicVariable.from('mode', index_js_1.VmType.Int8, rom == null || rom.length == 0 ? 0 : 1),
            index_js_1.VmNamedDynamicVariable.from('rom', index_js_1.VmType.Bytes, rom),
        ];
        seriesMetadataSchema.fields.forEach((s) => {
            // We don't verify default fields here, they are treated differently
            // in the code above.
            if (!metadata_helper_js_1.seriesDefaultMetadataFields.some((df) => df.name === s.name.data)) {
                (0, metadata_helper_js_1.pushMetadataField)(s, seriesMetadata, metadata);
            }
        });
        seriesMetadata.writeWithSchema(seriesMetadataSchema, wMetadata);
        return wMetadata.toUint8Array();
    }
}
exports.TokenSeriesMetadataBuilder = TokenSeriesMetadataBuilder;
