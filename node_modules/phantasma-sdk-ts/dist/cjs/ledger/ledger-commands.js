"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetBalanceFromMnemonic = exports.getBalanceFromMnemonic = exports.GetBalanceFromPrivateKey = exports.getBalanceFromPrivateKey = exports.SendTransactionLedger = exports.SignEncodedTx = exports.GetAddressFromLedeger = exports.getAddressFromLedger = exports.GetBalanceFromLedger = exports.getBalanceFromLedger = exports.GetLedgerSignerData = exports.GetLedgerAccountSigner = exports.getLedgerAccountSigner = exports.GetLedgerDeviceInfo = exports.getLedgerDeviceInfo = exports.ToWholeNumber = exports.toWholeNumber = exports.LeftPad = exports.leftPad = void 0;
exports.getLedgerSignerData = getLedgerSignerData;
exports.sendTransactionLedger = sendTransactionLedger;
const logger_js_1 = require("../utils/logger.js");
const index_js_1 = require("../tx/index.js");
const index_js_2 = require("../types/index.js");
const address_transcode_js_1 = require("./address-transcode.js");
const index_js_3 = require("../rpc/index.js");
const mnemonic_js_1 = require("./mnemonic.js");
const ledger_config_js_1 = require("./interfaces/ledger-config.js");
const transaction_sign_js_1 = require("./transaction-sign.js");
const transaction_transcode_js_1 = require("./transaction-transcode.js");
const ledger_utils_js_1 = require("./ledger-utils.js");
/**
 *
 * @param number
 * @param length
 * @returns
 */
const leftPad = (number, length) => {
    let str = '' + number;
    while (str.length < length) {
        str = '0' + str;
    }
    return str;
};
exports.leftPad = leftPad;
/** @deprecated Use `leftPad` instead. This alias will be removed in v1.0. */
exports.LeftPad = exports.leftPad;
/**
 *
 * @param balance
 * @param decimals
 * @returns
 */
const toWholeNumber = (balance, decimals) => {
    if (balance === undefined) {
        throw Error('balance is a required parameter.');
    }
    if (decimals === undefined) {
        throw Error('decimals is a required parameter.');
    }
    // console.log('toWholeNumber', 'balance', balance);
    const paddedBalance = (0, exports.leftPad)(balance, decimals + 1);
    // console.log('toWholeNumber', 'paddedBalance', paddedBalance);
    const prefixLength = paddedBalance.length - decimals;
    // console.log('toWholeNumber', 'prefixLength', prefixLength);
    const prefix = paddedBalance.slice(0, prefixLength);
    // console.log('toWholeNumber', 'prefix', prefix);
    const suffix = paddedBalance.slice(-decimals);
    // console.log('toWholeNumber', 'suffix', suffix);
    return `${prefix}.${suffix}`;
};
exports.toWholeNumber = toWholeNumber;
/** @deprecated Use `toWholeNumber` instead. This alias will be removed in v1.0. */
exports.ToWholeNumber = exports.toWholeNumber;
/**
 * Get the device info from the ledger.
 * @param config
 * @returns
 */
const getLedgerDeviceInfo = async (config) => {
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    const transport = (0, ledger_config_js_1.getLedgerTransport)(config);
    const version = await (0, ledger_utils_js_1.getVersion)(transport);
    const applicationName = await (0, ledger_utils_js_1.getApplicationName)(transport);
    return {
        version: version,
        applicationName: applicationName,
    };
};
exports.getLedgerDeviceInfo = getLedgerDeviceInfo;
/** @deprecated Use `getLedgerDeviceInfo` instead. This alias will be removed in v1.0. */
exports.GetLedgerDeviceInfo = exports.getLedgerDeviceInfo;
/**
 * Get Ledger Account Signer
 * @param config
 * @param accountIx
 * @returns
 */
const getLedgerAccountSigner = async (config, accountIx) => {
    /* istanbul ignore if */
    if (config === undefined) {
        throw Error('config is a required parameter.');
    }
    /* istanbul ignore if */
    if (accountIx === undefined) {
        throw Error('accountIx is a required parameter.');
    }
    const transport = (0, ledger_config_js_1.getLedgerTransport)(config);
    const paths = await transport.list();
    logger_js_1.logger.log('paths', paths);
    if (paths.length == 0) {
        if (typeof alert !== 'undefined') {
            alert('Number of devices found:' + paths.length);
        }
        throw Error('No Ledger device connected.');
    }
    const accountData = await getLedgerSignerData(config, {
        verifyOnDevice: false,
        debug: true,
    });
    const getSignerPublicKey = () => {
        if (!accountData.publicKey) {
            throw Error('Ledger did not return a public key.');
        }
        return accountData.publicKey;
    };
    const getSignerAccount = () => {
        if (!accountData.address) {
            throw Error('Ledger did not return an address.');
        }
        return accountData.address;
    };
    const signer = {
        getPublicKey: getSignerPublicKey,
        getAccount: getSignerAccount,
        GetPublicKey: getSignerPublicKey,
        GetAccount: getSignerAccount,
    };
    return signer;
};
exports.getLedgerAccountSigner = getLedgerAccountSigner;
/** @deprecated Use `getLedgerAccountSigner` instead. This alias will be removed in v1.0. */
exports.GetLedgerAccountSigner = exports.getLedgerAccountSigner;
/**
 * GetLedgerSignerData
 * @param config
 * @param options
 * @returns
 */
async function getLedgerSignerData(config, options) {
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    if (options == undefined) {
        throw Error('options is a required parameter.');
    }
    const msg = await (0, ledger_utils_js_1.getPublicKey)((0, ledger_config_js_1.getLedgerTransport)(config), options);
    const response = {
        address: index_js_2.Address.nullAddress,
        publicKey: '',
        success: false,
        message: '',
    };
    response.success = false;
    response.message = msg.message;
    if (!msg.success) {
        return response;
    }
    const publicKey = msg.publicKey;
    if (!publicKey) {
        response.message = 'Ledger did not return a public key.';
        return response;
    }
    const address = (0, address_transcode_js_1.getAddressPublicKeyFromPublicKey)(publicKey);
    response.success = true;
    response.message = 'success';
    response.address = address;
    response.publicKey = publicKey;
    return response;
}
/** @deprecated Use `getLedgerSignerData` instead. This alias will be removed in v1.0. */
exports.GetLedgerSignerData = getLedgerSignerData;
/**
 * GetBalanceFromLedger
 * @param config
 * @param options
 * @returns
 */
const getBalanceFromLedger = async (config, options) => {
    /* istanbul ignore if */
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    /* istanbul ignore if */
    if (options == undefined) {
        throw Error('options is a required parameter.');
    }
    const debug = (0, ledger_config_js_1.getLedgerDebug)(config);
    const rpc = (0, ledger_config_js_1.getLedgerRpc)(config);
    const msg = await (0, ledger_utils_js_1.getPublicKey)((0, ledger_config_js_1.getLedgerTransport)(config), options);
    /* istanbul ignore if */
    if (debug) {
        logger_js_1.logger.log('getBalanceFromLedger', 'msg', msg);
    }
    const response = {
        address: index_js_2.Address.nullAddress,
        publicKey: '',
        balances: new Map(),
        success: false,
        message: '',
    };
    response.message = msg.message;
    if (!msg.success) {
        return response;
    }
    const publicKey = msg.publicKey;
    if (!publicKey) {
        response.message = 'Ledger did not return a public key.';
        return response;
    }
    const address = (0, address_transcode_js_1.getAddressPublicKeyFromPublicKey)(publicKey);
    /* istanbul ignore if */
    if (debug) {
        logger_js_1.logger.log('address', address);
        logger_js_1.logger.log('rpc', rpc);
    }
    const rpcResponse = await rpc.getAccount(address.text);
    if (debug) {
        logger_js_1.logger.log('rpcResponse', rpcResponse);
    }
    if ((0, index_js_3.isRpcErrorResult)(rpcResponse)) {
        response.message = rpcResponse.error;
        return response;
    }
    response.balances = new Map();
    if (rpcResponse.balances !== undefined) {
        rpcResponse.balances.forEach((balanceElt) => {
            response.balances?.set(balanceElt.symbol, (0, exports.toWholeNumber)(balanceElt.amount, balanceElt.decimals));
        });
    }
    response.address = address;
    response.publicKey = publicKey;
    response.success = true;
    return response;
};
exports.getBalanceFromLedger = getBalanceFromLedger;
/** @deprecated Use `getBalanceFromLedger` instead. This alias will be removed in v1.0. */
exports.GetBalanceFromLedger = exports.getBalanceFromLedger;
/**
 * Get Addres from Ledger
 * @param config
 * @param options
 * @returns
 */
const getAddressFromLedger = async (config, options) => {
    /* istanbul ignore if */
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    /* istanbul ignore if */
    if (options == undefined) {
        throw Error('options is a required parameter.');
    }
    const msg = await (0, ledger_utils_js_1.getPublicKey)((0, ledger_config_js_1.getLedgerTransport)(config), options);
    /* istanbul ignore if */
    if ((0, ledger_config_js_1.getLedgerDebug)(config)) {
        logger_js_1.logger.log('getBalanceFromLedger', 'msg', msg);
    }
    if (msg.success) {
        const publicKey = msg.publicKey;
        if (!publicKey) {
            return { success: false, message: 'Ledger did not return a public key.' };
        }
        const address = (0, address_transcode_js_1.getAddressFromPublicKey)(publicKey);
        return address;
    }
    else {
        return msg;
    }
};
exports.getAddressFromLedger = getAddressFromLedger;
/** @deprecated Use `getAddressFromLedger` instead. This typoed alias will be removed in v1.0. */
exports.GetAddressFromLedeger = exports.getAddressFromLedger;
/**
 *
 * @param encodedTx
 * @param config
 * @returns
 */
async function signEncodedTx(encodedTx, config) {
    const response = await (0, ledger_utils_js_1.signLedger)((0, ledger_config_js_1.getLedgerTransport)(config), encodedTx);
    /* istanbul ignore if */
    if ((0, ledger_config_js_1.getLedgerDebug)(config)) {
        logger_js_1.logger.log('sendAmountUsingLedger', 'signCallback', 'response', response);
    }
    if (response.success) {
        if (!response.signature) {
            throw Error('Ledger reported signing success without returning a signature.');
        }
        return response.signature;
    }
    else {
        throw Error(response.message);
    }
}
/** @deprecated Use internal `signEncodedTx` flow instead. This alias will be removed in v1.0. */
exports.SignEncodedTx = signEncodedTx;
/**
 * SendTransactionLedger
 * @param config
 * @param script
 * @returns
 */
async function sendTransactionLedger(config, script) {
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    const debug = (0, ledger_config_js_1.getLedgerDebug)(config);
    const rpc = (0, ledger_config_js_1.getLedgerRpc)(config);
    const options = { verifyOnDevice: false };
    const msg_publicKey = await (0, ledger_utils_js_1.getPublicKey)((0, ledger_config_js_1.getLedgerTransport)(config), options);
    if (!msg_publicKey.success) {
        if (debug) {
            logger_js_1.logger.log('SendTransactionLedger', 'error ', msg_publicKey);
        }
        return msg_publicKey;
    }
    const publicKey = msg_publicKey.publicKey;
    const nexusName = (0, ledger_config_js_1.getLedgerNexusName)(config);
    const chainName = (0, ledger_config_js_1.getLedgerChainName)(config);
    const expirationDate = (0, transaction_transcode_js_1.getExpirationDate)();
    // no payload, could be a message.
    const payload = (0, ledger_config_js_1.getLedgerPayload)(config);
    const myTransaction = new index_js_1.Transaction(nexusName, // Nexus Name
    chainName, // Chain
    script, // In string format
    expirationDate, // Expiration Date
    payload); // Extra Info to attach to Transaction in Serialized Hex
    const encodedTx = index_js_2.Base16.encodeUint8Array(myTransaction.toByteArray(false));
    try {
        if (debug) {
            logger_js_1.logger.log('sendAmountUsingCallback', 'encodedTx', encodedTx);
        }
        const signature = await signEncodedTx(encodedTx, config);
        if (debug) {
            logger_js_1.logger.log('sendAmountUsingCallback', 'signature', signature);
        }
        if ((0, ledger_config_js_1.getLedgerVerifyResponse)(config)) {
            const verifyResponse = (0, transaction_sign_js_1.verify)(encodedTx, signature, publicKey);
            if (verifyResponse == false) {
                throw Error(`invalidSignature encodedTx:'${encodedTx}', publicKey:'${publicKey}' signature:'${signature}'`);
            }
            if (debug) {
                logger_js_1.logger.log('verifyResponse', verifyResponse);
            }
        }
        const signatureBytes = index_js_2.Base16.decodeUint8Array(signature);
        const mySignature = new index_js_2.Ed25519Signature(signatureBytes);
        const myNewSignaturesArray = [];
        myNewSignaturesArray.push(mySignature);
        myTransaction.signatures = myNewSignaturesArray;
        if (debug) {
            logger_js_1.logger.log('signedTx', myTransaction);
        }
        const encodedSignedTx = index_js_2.Base16.encodeUint8Array(myTransaction.toByteArray(true));
        logger_js_1.logger.log('encoded signed tx: ', encodedSignedTx);
        const txHash = await rpc.sendRawTransaction(encodedSignedTx);
        if (debug) {
            logger_js_1.logger.log('sendAmountUsingCallback', 'txHash', txHash);
        }
        if ((0, index_js_3.isRpcErrorResult)(txHash)) {
            return {
                success: false,
                message: txHash.error,
            };
        }
        const response = {
            success: true,
            message: txHash,
        };
        /* istanbul ignore if */
        if (debug) {
            logger_js_1.logger.log('response', response);
        }
        return response;
    }
    catch (error) {
        if (debug) {
            logger_js_1.logger.log('error', error);
        }
        const errorResponse = {
            success: false,
            message: error instanceof Error ? error.message : String(error),
        };
        return errorResponse;
    }
}
/** @deprecated Use `sendTransactionLedger` instead. This alias will be removed in v1.0. */
exports.SendTransactionLedger = sendTransactionLedger;
/**
 *
 * @param config
 * @param privateKey
 * @returns
 */
const getBalanceFromPrivateKey = async (config, privateKey) => {
    /* istanbul ignore if */
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    /* istanbul ignore if */
    if (privateKey == undefined) {
        throw Error('privateKey is a required parameter.');
    }
    /* istanbul ignore if */
    const debug = (0, ledger_config_js_1.getLedgerDebug)(config);
    const rpc = (0, ledger_config_js_1.getLedgerRpc)(config);
    if (debug) {
        logger_js_1.logger.log('privateKey', privateKey);
    }
    // https://github.com/phantasma-io/phantasma-sdk-ts/blob/7d04aaed839851ae5640f68ab223ca7d92c42016/core/tx/utils.js
    const publicKey = (0, transaction_sign_js_1.getPublicFromPrivate)(privateKey);
    /* istanbul ignore if */
    if (debug) {
        logger_js_1.logger.log('publicKey', publicKey);
    }
    const address = (0, address_transcode_js_1.getAddressFromPublicKey)(publicKey);
    /* istanbul ignore if */
    if (debug) {
        logger_js_1.logger.log('address', address);
    }
    // const path = `/address/${address}`;
    // const response = await httpRequestUtil.get(config, path);
    const rpcResponse = await rpc.getAccount(address);
    if (debug) {
        logger_js_1.logger.log('rpcResponse', rpcResponse);
    }
    const response = {
        address: index_js_2.Address.nullAddress,
        publicKey,
        balances: new Map(),
        success: false,
        message: '',
    };
    if ((0, index_js_3.isRpcErrorResult)(rpcResponse)) {
        response.message = rpcResponse.error;
        return response;
    }
    if (rpcResponse.balances !== undefined) {
        rpcResponse.balances.forEach((balanceElt) => {
            response.balances?.set(balanceElt.symbol, (0, exports.toWholeNumber)(balanceElt.amount, balanceElt.decimals));
        });
    }
    response.address = index_js_2.Address.fromText(address);
    response.success = true;
    // const lastRefPath = `/transaction/last-ref/${address}`;
    // const lastRefResponse = await httpRequestUtil.get(config, lastRefPath);
    // response.lastRef = lastRefResponse;
    return response;
};
exports.getBalanceFromPrivateKey = getBalanceFromPrivateKey;
/** @deprecated Use `getBalanceFromPrivateKey` instead. This alias will be removed in v1.0. */
exports.GetBalanceFromPrivateKey = exports.getBalanceFromPrivateKey;
/**
 *
 * @param config
 * @param mnemonic
 * @param index
 * @returns
 */
const getBalanceFromMnemonic = async (config, mnemonic, index) => {
    /* istanbul ignore if */
    if (config == undefined) {
        throw Error('config is a required parameter.');
    }
    /* istanbul ignore if */
    if (mnemonic == undefined) {
        throw Error('mnemonic is a required parameter.');
    }
    /* istanbul ignore if */
    if (index == undefined) {
        throw Error('index is a required parameter.');
    }
    /* istanbul ignore if */
    if ((0, ledger_config_js_1.getLedgerDebug)(config)) {
        logger_js_1.logger.log('mnemonic', mnemonic);
    }
    const privateKey = (0, mnemonic_js_1.getPrivateKeyFromMnemonic)(config, mnemonic, index);
    return await (0, exports.getBalanceFromPrivateKey)(config, privateKey);
};
exports.getBalanceFromMnemonic = getBalanceFromMnemonic;
/** @deprecated Use `getBalanceFromMnemonic` instead. This alias will be removed in v1.0. */
exports.GetBalanceFromMnemonic = exports.getBalanceFromMnemonic;
