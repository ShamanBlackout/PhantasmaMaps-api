"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setLogger = exports.logger = void 0;
const noop = () => { };
// Default SDK logger: silent unless the consumer explicitly enables logging.
exports.logger = {
    log: noop,
    info: noop,
    warn: noop,
    error: noop,
    debug: noop,
    trace: noop,
};
// Use a custom logger (for example, setLogger(console)) to enable output.
function setLogger(custom) {
    exports.logger.log = custom?.log ?? noop;
    exports.logger.info = custom?.info ?? noop;
    exports.logger.warn = custom?.warn ?? noop;
    exports.logger.error = custom?.error ?? noop;
    exports.logger.debug = custom?.debug ?? noop;
    exports.logger.trace = custom?.trace ?? custom?.debug ?? custom?.log ?? noop;
}
exports.setLogger = setLogger;
