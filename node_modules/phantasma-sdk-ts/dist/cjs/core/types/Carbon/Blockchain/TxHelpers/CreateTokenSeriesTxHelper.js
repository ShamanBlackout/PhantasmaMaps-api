"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreateTokenSeriesTxHelper = void 0;
const index_js_1 = require("../../../../utils/index.js");
const CarbonSerialization_js_1 = require("../../../CarbonSerialization.js");
const Bytes32_js_1 = require("../../Bytes32.js");
const SmallString_js_1 = require("../../SmallString.js");
const TxTypes_js_1 = require("../../TxTypes.js");
const TxMsgSigner_js_1 = require("../Extensions/TxMsgSigner.js");
const ModuleId_js_1 = require("../ModuleId.js");
const index_js_2 = require("../Modules/index.js");
const TxMsg_js_1 = require("../TxMsg.js");
const TxMsgCall_js_1 = require("../TxMsgCall.js");
const FeeOptions_js_1 = require("./FeeOptions.js");
class CreateTokenSeriesTxHelper {
    /** Build a Tx without signing. */
    static buildTx(tokenId, // ulong
    seriesInfo, creatorPublicKey, feeOptions, maxData, expiry) {
        const fees = feeOptions ?? new FeeOptions_js_1.CreateSeriesFeeOptions();
        const maxGas = fees.calculateMaxGas();
        const argsW = new CarbonSerialization_js_1.CarbonBinaryWriter();
        argsW.write8(tokenId);
        seriesInfo.write(argsW);
        // --- Tx message: Call(Token.CreateTokenSeries, args) ---
        const msg = new TxMsg_js_1.TxMsg();
        msg.type = TxTypes_js_1.TxTypes.Call;
        msg.expiry = expiry || BigInt(Date.now() + 60000);
        msg.maxGas = maxGas;
        msg.maxData = maxData;
        msg.gasFrom = creatorPublicKey;
        msg.payload = SmallString_js_1.SmallString.empty;
        const call = new TxMsgCall_js_1.TxMsgCall();
        call.moduleId = ModuleId_js_1.ModuleId.Token;
        call.methodId = index_js_2.TokenContract_Methods.CreateTokenSeries;
        call.args = argsW.toUint8Array();
        msg.msg = call;
        return msg;
    }
    /** Build and sign, returning raw bytes. */
    static buildTxAndSign(tokenId, seriesInfo, signer, feeOptions, maxData, expiry) {
        const tx = this.buildTx(tokenId, seriesInfo, new Bytes32_js_1.Bytes32(signer.PublicKey), feeOptions, maxData, expiry);
        return TxMsgSigner_js_1.TxMsgSigner.signAndSerialize(tx, signer);
    }
    /** Build, sign and return hex string. */
    static buildTxAndSignHex(tokenId, seriesInfo, signer, feeOptions, maxData, expiry) {
        return (0, index_js_1.bytesToHex)(this.buildTxAndSign(tokenId, seriesInfo, signer, feeOptions, maxData, expiry));
    }
    static parseResult(resultHex) {
        // UInt32 carbon seriesId
        const r = new CarbonSerialization_js_1.CarbonBinaryReader((0, index_js_1.hexToBytes)(resultHex));
        return r.read4u();
    }
}
exports.CreateTokenSeriesTxHelper = CreateTokenSeriesTxHelper;
