"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TxMsgSpecialResolution = void 0;
const TxMsgCall_js_1 = require("./TxMsgCall.js");
class TxMsgSpecialResolution {
    constructor(resolutionId = 0n, calls = []) {
        this.resolutionId = resolutionId;
        this.calls = calls;
    }
    write(w) {
        w.write8u(this.resolutionId);
        w.writeArrayBlob(this.calls);
    }
    read(r) {
        this.resolutionId = r.read8u();
        this.calls = r.readArrayBlob(TxMsgCall_js_1.TxMsgCall);
    }
    static read(r) {
        const v = new TxMsgSpecialResolution();
        v.read(r);
        return v;
    }
}
exports.TxMsgSpecialResolution = TxMsgSpecialResolution;
