"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NftRomBuilder = void 0;
const CarbonSerialization_js_1 = require("../../../../CarbonSerialization.js");
const index_js_1 = require("../../Vm/index.js");
const StandardMeta_js_1 = require("../StandardMeta.js");
const MetadataHelper_js_1 = require("./MetadataHelper.js");
class NftRomBuilder {
    static buildAndSerialize(nftRomSchema, phantasmaNftId, metadata) {
        const wRom = new CarbonSerialization_js_1.CarbonBinaryWriter();
        const romField = (0, MetadataHelper_js_1.findMetadataField)(metadata, 'rom');
        let rom;
        if (romField) {
            if (!(romField.value instanceof Uint8Array || typeof romField.value === "string")) {
                throw Error("'rom' must be a byte array or hex string");
            }
            rom = romField.value;
        }
        const nftRom = new index_js_1.VmDynamicStruct();
        nftRom.fields = [
            index_js_1.VmNamedDynamicVariable.from(StandardMeta_js_1.StandardMeta.id, index_js_1.VmType.Int256, phantasmaNftId),
            index_js_1.VmNamedDynamicVariable.from('rom', index_js_1.VmType.Bytes, rom ? rom : [])
        ];
        nftRomSchema.fields.forEach(s => {
            // We don't verify default fields here, they are treated differently
            // in the code above.
            if (!MetadataHelper_js_1.nftDefaultMetadataFields.some(df => df.name === s.name.data)) {
                (0, MetadataHelper_js_1.pushMetadataField)(s, nftRom, metadata);
            }
        });
        nftRom.writeWithSchema(nftRomSchema, wRom);
        // console.log("nftRom.fields.length: ", nftRom.fields.length);
        // TokenSchemasBuilder.logSchema(nftRomSchema);
        return wRom.toUint8Array();
    }
}
exports.NftRomBuilder = NftRomBuilder;
