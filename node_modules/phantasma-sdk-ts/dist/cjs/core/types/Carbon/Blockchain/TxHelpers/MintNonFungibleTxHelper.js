"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MintNonFungibleTxHelper = void 0;
const index_js_1 = require("../../../../utils/index.js");
const CarbonSerialization_js_1 = require("../../../CarbonSerialization.js");
const Bytes32_js_1 = require("../../Bytes32.js");
const SmallString_js_1 = require("../../SmallString.js");
const TxTypes_js_1 = require("../../TxTypes.js");
const TxMsgSigner_js_1 = require("../Extensions/TxMsgSigner.js");
const TokenHelper_js_1 = require("../Modules/TokenHelper.js");
const TxMsg_js_1 = require("../TxMsg.js");
const TxMsgMintNonFungible_js_1 = require("../TxMsgMintNonFungible.js");
const FeeOptions_js_1 = require("./FeeOptions.js");
class MintNonFungibleTxHelper {
    // Build a Tx without signing
    static buildTx(carbonTokenId, carbonSeriesId, senderPublicKey, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const fees = feeOptions ?? new FeeOptions_js_1.MintNftFeeOptions();
        const maxGas = fees.calculateMaxGas();
        const msg = new TxMsg_js_1.TxMsg();
        msg.type = TxTypes_js_1.TxTypes.MintNonFungible;
        msg.expiry = expiry || BigInt(Date.now() + 60000);
        msg.maxGas = maxGas;
        msg.maxData = maxData;
        msg.gasFrom = senderPublicKey;
        msg.payload = SmallString_js_1.SmallString.empty;
        const mint = new TxMsgMintNonFungible_js_1.TxMsgMintNonFungible();
        mint.tokenId = carbonTokenId;
        mint.seriesId = carbonSeriesId;
        mint.to = receiverPublicKey;
        mint.rom = rom;
        mint.ram = ram;
        msg.msg = mint;
        return msg;
    }
    // Build and sign, returning raw bytes
    static buildTxAndSign(tokenId, seriesId, signer, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const senderPub = new Bytes32_js_1.Bytes32(signer.PublicKey);
        const tx = this.buildTx(tokenId, seriesId, senderPub, receiverPublicKey, rom, ram, feeOptions, maxData, expiry);
        return TxMsgSigner_js_1.TxMsgSigner.signAndSerialize(tx, signer);
    }
    // Build, sign and return hex string
    static buildTxAndSignHex(tokenId, seriesId, signer, receiverPublicKey, rom, ram, feeOptions, maxData, expiry) {
        const bytes = this.buildTxAndSign(tokenId, seriesId, signer, receiverPublicKey, rom, ram, feeOptions, maxData, expiry);
        return (0, index_js_1.bytesToHex)(bytes);
    }
    static parseResult(carbonTokenId, resultHex) {
        const result = [];
        const r = new CarbonSerialization_js_1.CarbonBinaryReader((0, index_js_1.hexToBytes)(resultHex));
        const count = r.read4u();
        for (let i = 0; i < count; i++) {
            const instanceId = r.read8u();
            const carbonNftId = TokenHelper_js_1.TokenHelper.getNftAddress(carbonTokenId, instanceId);
            result.push(carbonNftId);
        }
        return result;
    }
}
exports.MintNonFungibleTxHelper = MintNonFungibleTxHelper;
