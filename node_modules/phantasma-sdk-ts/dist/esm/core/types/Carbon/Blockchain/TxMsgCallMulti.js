import { TxMsgCall } from './TxMsgCall.js';
export class TxMsgCallMulti {
    constructor(calls = []) {
        this.calls = calls;
    }
    write(w) {
        w.writeArrayBlob(this.calls);
    }
    read(r) {
        this.calls = r.readArrayBlob(TxMsgCall);
    }
    static read(r) {
        const v = new TxMsgCallMulti();
        v.read(r);
        return v;
    }
}
