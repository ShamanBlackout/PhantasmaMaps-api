import { IntX } from '../IntX.js';
export class TxMsgBurnFungible {
    constructor(init) {
        this.tokenId = 0n;
        this.amount = IntX.fromI64(0);
        Object.assign(this, init);
    }
    write(w) {
        w.write8u(this.tokenId);
        this.amount.write(w);
    }
    read(r) {
        this.tokenId = r.read8u();
        this.amount = IntX.read(r);
    }
    static read(r) {
        const v = new TxMsgBurnFungible();
        v.read(r);
        return v;
    }
}
