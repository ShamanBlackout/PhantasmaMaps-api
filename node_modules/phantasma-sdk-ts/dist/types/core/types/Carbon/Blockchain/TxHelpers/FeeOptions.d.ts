/** Common interface for fee option. */
export interface IFeeOptions {
    feeMultiplier: bigint;
    calculateMaxGas(...args: unknown[]): bigint;
}
/** Base fee options with sensible defaults. */
export declare class FeeOptions implements IFeeOptions {
    gasFeeBase: bigint;
    feeMultiplier: bigint;
    constructor(gasFeeBase?: bigint, feeMultiplier?: bigint);
    calculateMaxGas(..._args: unknown[]): bigint;
}
/** Fee options for token creation transactions. */
export declare class CreateTokenFeeOptions extends FeeOptions implements IFeeOptions {
    gasFeeCreateTokenBase: bigint;
    gasFeeCreateTokenSymbol: bigint;
    constructor(gasFeeBase?: bigint, gasFeeCreateTokenBase?: bigint, gasFeeCreateTokenSymbol?: bigint, feeMultiplier?: bigint);
    calculateMaxGas(...args: unknown[]): bigint;
}
/** Fee options for creating a new series on an NFT token. */
export declare class CreateSeriesFeeOptions extends FeeOptions implements IFeeOptions {
    gasFeeCreateSeriesBase: bigint;
    constructor(gasFeeBase?: bigint, gasFeeCreateSeriesBase?: bigint, feeMultiplier?: bigint);
    calculateMaxGas(..._args: unknown[]): bigint;
}
/** Fee options for minting non-fungible tokens (NFT instances). */
export declare class MintNftFeeOptions extends FeeOptions implements IFeeOptions {
    constructor(gasFeeBase?: bigint, feeMultiplier?: bigint);
    calculateMaxGas(..._args: unknown[]): bigint;
}
//# sourceMappingURL=FeeOptions.d.ts.map